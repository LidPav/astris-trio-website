---
layout: layout.njk
title: Repertoire
description: Repertoire des Astris Trio
---

<section class="editorial">
  <div class="editorial__header">
    <h1 class="editorial__title">Repertoire</h1>
    <p class="editorial__lead">
      Das Repertoire des Astris Trios umfasst Werke verschiedenster Epochen – von der Klassik
      bis hin zur zeitgenössischen Musik – und spiegelt die stilistische Vielfalt und Offenheit
      des Ensembles wider.
    </p>
  </div>

  <div class="editorial__grid">
    <div class="editorial__main">

      <h2>Repertoire &amp; Programme</h2>

      {# Reihenfolge der Epochen bewusst festgelegt #}
      {% set epochOrder = ['Klassik', 'Romantik', 'Moderne', 'Zeitgenössisch'] %}

      <section id="repertoire" class="repertoire">
        {% for epoch in epochOrder %}

          {# Komponisten herausfiltern, die zu dieser Epoche gehören.
             Hinweis: Object.keys() Reihenfolge folgt der JSON-Einfügereihenfolge. #}
          {% set composersInEpoch = [] %}
          {% for composer, data in repertoireByComposer %}
            {% if data.epoch == epoch %}
              {% set composersInEpoch = composersInEpoch.concat([{ 'name': composer, 'works': data.works }]) %}
            {% endif %}
          {% endfor %}

          {# Nur Abschnitt ausgeben, wenn es Inhalte gibt #}
          {% if composersInEpoch.length %}
            <h3 class="repertoire__epoch">{{ epoch }}</h3>

            {% for c in composersInEpoch %}
              <h4>{{ c.name }}</h4>
              <ul>
                {% for work in c.works %}
                  <li>{{ work }}</li>
                {% endfor %}
              </ul>
            {% endfor %}

          {% endif %}

        {% endfor %}
      </section>

    </div>

    <aside class="editorial__aside">
      <div class="aside-card">
        <h3>Programme</h3>
        <p class="muted">
          Gerne stellen wir thematische Programme zusammen
          (z.&nbsp;B. Schubert–Beethoven, Wiener Klassik, Moderne).
        </p>
      </div>

      <div class="aside-card">
        <h3>Besetzungen</h3>
        <p class="muted">
          Klavier · Violine · Violoncello<br>
          (auf Anfrage auch mit Gästen)
        </p>
      </div>
    </aside>
  </div>
</section>
