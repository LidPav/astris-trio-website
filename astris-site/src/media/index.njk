---
layout: layout.njk
title: Medien
description: Videos & Aufnahmen
---

<h1>Medien</h1>
<p class="lead">Ausgewählte Videoaufnahmen</p>

<div class="list">
  {% for post in collections.konzert | reverse %}
    {% if post.data.video %}
      <div class="card">

        <h2 class="h2">{{ post.data.title }}</h2>
        <p class="muted small">
          {{ post.data.date }} · {{ post.data.city }} · {{ post.data.venue }}
        </p>

        <div class="video-embed">
          <iframe
            src="https://www.youtube.com/embed/{{ post.data.video | replace('https://www.youtube.com/watch?v=', '') }}"
            title="YouTube Video"
            frameborder="0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen>
          </iframe>
        </div>

      </div>
    {% endif %}
  {% endfor %}
</div>
