---
layout: layout.njk
title: "Musiker:innen"
---

<h1>Musiker:innen</h1>

<div class="musiker-grid">
  {% for m in musiker.members %}
    <article class="musiker-card">
      <img src="{{ m.image | url }}" alt="{{ m.name }}" class="musiker-card__image">
      <h2>{{ m.name }}</h2>
      <p class="muted">{{ m.instrument }}</p>
      <p>{{ m.bio }}</p>
    </article>
  {% endfor %}
</div>
