---
layout: layout.njk
title: Musiker:innen
description: Mitglieder des Astris Trio
---

<h1 class="h1">Musiker:innen</h1>
<p class="lead">Kurzbiografien (für die ausführlichen Versionen können wir optional eine Download-PDF ergänzen).</p>

<div class="list">
  {% for m in musiker.members %}
    <div class="card">
      <div class="person">
        <img src="{{ m.image }}" alt="{{ m.name }}" loading="lazy" />
        <div>
          <div class="person__name">{{ m.name }}</div>
          <div class="person__role">{{ m.instrument }}</div>
        </div>
      </div>
      <p class="small" style="margin-top:12px">{{ m.bio }}</p>
    </div>
  {% endfor %}
</div>
