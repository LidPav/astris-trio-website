---
layout: layout.njk
title: Konzerte
description: Termine und Konzertarchiv
---

<h1 class="h1">Konzerte</h1>
<p class="lead">Termine &amp; Archiv</p>

<div class="list">
  {# Nunjucks: sortiere nach Datum und dann umdrehen (neueste zuerst) #}
  {% set items = collections.konzerte | sort('date') | reverse %}
  {% for post in items %}
    <div class="card">

      <h2 class="h2">
        <a href="{{ post.url | url }}">{{ post.data.title }}</a>
      </h2>

      <p class="muted small">
        {{ post.date | deDate("EEEE, d. LLLL yyyy") }}
        {% if post.data.city %} · {{ post.data.city }}{% endif %}
        {% if post.data.venue %} · {{ post.data.venue }}{% endif %}
      </p>

      <a class="btn" href="{{ post.url | url }}">Details</a>
    </div>
  {% endfor %}
</div>
